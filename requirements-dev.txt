-r requirements.txt

# Тестирование
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1             # для тестового клиента (уже есть, но дублируем)

# Линтеры и форматтеры (по желанию)
ruff==0.8.4
black==24.10.0
mypy==1.13.0

# Утилиты
ipython==8.29.0           # для интерактивной отладки
