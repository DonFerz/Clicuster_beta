# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1
psycopg2-binary==2.9.12          # для sync-операций Alembic (если не используете — удалите)

# --- Settings & validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
email-validator==2.2.0

# --- Auth ---
python-jose[cryptography]==3.3.0
bcrypt==5.0.0
python-multipart==0.0.20

# --- Cache & background tasks ---
redis==5.2.1
arq==0.26.1

# --- Email ---
aiosmtplib==4.0.1

# --- HTTP client (для внешних API) ---
httpx==0.28.1

# --- Timezone data (обязательно на Windows для Alembic и zoneinfo) ---
tzdata==2026.4
